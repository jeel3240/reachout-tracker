# web

Next.js dashboard for the outreach tracker. See the [root README](../README.md) for setup.

```bash
cp .env.example .env.local   # fill in SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, optional APP_PASSWORD
npm run dev                   # http://localhost:3000
```
